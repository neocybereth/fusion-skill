# Agent Notes

- The runtime skill contract is `skills/fusion/SKILL.md`.
- Keep the skill lean. Put optional detail in `skills/fusion/references/`.
- Validate changes with `python3 scripts/validate.py` and the Codex skill validator before publishing.
- Do not add generated `.fusion/` run artifacts to commits.
